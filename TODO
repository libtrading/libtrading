- Don't do direct malloc() - we need to able to use mlock()'d memory pool
- TLSF - 'realtime' malloc()
- xread()/xwrite()
- make check requires clean
- Licensing (GPL + linking exception, BSD?)
- Fix overflows in message parsing
- TCP_NODELAY
- Latency test
- use writev() for head_buf and body_buf
